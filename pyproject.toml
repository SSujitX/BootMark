[project]
name = "bootmark"
version = "1.0.0"
description = "BIOS Backup / Validation / Flash Assistant for Windows"
readme = "README.md"
requires-python = ">=3.13.12"
dependencies = [
    "pyinstaller>=6.20.0",
    "pyqt6>=6.11.0",
]
